# 현재 에셋(픽셀 스프라이트)은 KB 단위로 작아 일반 git으로 관리합니다.
# git-lfs가 설치돼 있지 않으므로 LFS 필터는 비활성화 상태입니다.
# 추후 대용량 에셋(오디오/3D/고해상도 텍스처)이 생기면 git-lfs 설치 후
# 아래 같은 규칙을 추가하세요:
#   *.wav  filter=lfs diff=lfs merge=lfs -text
#   *.ogg  filter=lfs diff=lfs merge=lfs -text
#   *.glb  filter=lfs diff=lfs merge=lfs -text
